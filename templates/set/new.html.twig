{% extends 'base.html.twig' %}

{% block title %}New Set - LEGO Inventory
{% endblock %}

{% block body %}
	<div class="card">
		<div class="card-header">
			<h1 style="margin: 0; font-size: 1.5rem;">Add a New Set</h1>
		</div>
		<div class="card-body">
			{{ include('set/_form.html.twig') }}
		</div>
	</div>

	<div style="margin-top: 1rem;">
		<a href="{{ path('app_set_index') }}" class="btn btn-secondary">Back to Dashboard</a>
	</div>
{% endblock %}
