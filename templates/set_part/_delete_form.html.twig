<form method="post" action="{{ path('app_set_part_delete', {'id': set_part.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ set_part.id) }}">
    <button class="btn">Delete</button>
</form>
