{% extends 'base.html.twig' %}

{% block title %}MissingPart{% endblock %}

{% block body %}
    <h1>MissingPart</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ missing_part.id }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ missing_part.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_missing_part_index') }}">back to list</a>

    <a href="{{ path('app_missing_part_edit', {'id': missing_part.id}) }}">edit</a>

    {{ include('missing_part/_delete_form.html.twig') }}
{% endblock %}
