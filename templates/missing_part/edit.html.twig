{% extends 'base.html.twig' %}

{% block title %}Edit Missing Part
{% endblock %}

{% block body %}
	<div class="page-header">
		<h1 class="page-title">
			Edit Missing Part
		</h1>
	</div>

	<div class="card">
		<div class="card-body">
			{{ include('missing_part/_form.html.twig', {'button_label': 'Update'}) }}
		</div>
	</div>

	<div class="flex-between mt-3">
		<a href="{{ path('app_missing_part_index') }}" class="btn btn-secondary">
			Back to list
		</a>
		{{ include('missing_part/_delete_form.html.twig') }}
	</div>
{% endblock %}
