<form method="post" action="{{ path('app_missing_part_delete', {'id': missing_part.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ missing_part.id) }}">
	<button class="btn btn-small btn-danger">Delete</button>
</form>
